const numbers = [1, 2, 3];
const dblNumbers = numbers.map((num) => num * 2); // does something to each input of an array then returns a new array -- in this case multiplies the number by 2

console.log(numbers); // Output: [1, 2, 3]
console.log(dblNumbers); // Output: [2, 4, 6]
